export interface MenuResponseDto {
    _id: string;
    name: string;
    image?: string;
    description: string;
    price: number;
    storeId: string;
    createdAt: string;
    updatedAt: string;
}
